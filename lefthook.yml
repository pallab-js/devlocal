pre-commit:
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check
      working_directory: src-tauri
    clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings
      working_directory: src-tauri
    ts-check:
      glob: "*.{ts,tsx}"
      run: pnpm lint
